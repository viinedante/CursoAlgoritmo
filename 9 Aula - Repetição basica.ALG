algoritmo "ConteAte10"
var
   Contador: Inteiro

inicio
   Contador <- 0
   Enquanto (Contador <= 10) faca
      EscrevaL (Contador)
      Contador <- Contador + 1
   FimEnquanto
   Escreval (" Terminie de contar ")


fimalgoritmo